module.exports = {
  // Define the entry for the server-side rendering build
  serverBuildTarget: "vercel", // Tells Remix to use the Vercel adapter
  // Add more configuration if needed (like routes, dev settings, etc.)
};


// This is part of vercel